
{
    "builds": [{
        "src": "prisonsystem/wsgi.py",
        "use": "@natanffs/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "prisonsystem/wsgi.py"
        }
    ]
}